---
title: "MDX Styling Examples"
description: "Complete examples of all MDX content styles and formatting"
slug: "demo"
icon: "FileText"
prevButtonText: "Back"
nextButtonText: "Forward"
locked: true
---

# MDX Content Styling Examples
slug: "mdx-content-styling-examples"

This page demonstrates all available styles and formatting for MDX content, including syntax-highlighted code.

## Headings of All Levels
slug: "headings-first"

### Third Level Heading
slug: "third-level-heading"

#### Fourth Level Heading
slug: "fourth-level-heading"

##### Fifth Level Heading
slug: "fifth-level-heading"

###### Sixth Level Heading
slug: "sixth-level-heading"

## Text Formatting
slug: "text-formatting"

This is **bold text**, this is <u>underlined text</u>, and this is *italic*. You can also use ***bold italic***, **<u>bold underlined</u>** and ***<u>bold underlined italic</u>***.

~~Strikethrough text~~ and <mark>highlighted text</mark>.

`Inline code` in text.

## Lists

### Bulleted List

- First list item
- Second list item
  - Nested item
  - Another nested item
- Third list item

### Numbered List

1. First item
2. Second item
   1. Nested item
   2. Another nested item
3. Third item

### Mixed Bulleted and Numbered List

*   **Highlight:** Description:
    1.  Item 1.
    2.  Item 2.
    3.  Item 3.
*   **Highlight:** End.

### Task List

- [x] Completed task
- [ ] Uncompleted task
- [x] Another completed task

## Blockquotes

> This is a regular blockquote. It can contain multiple lines of text.
> 
> And even multiple paragraphs.

> ### Blockquote with Heading
> 
> A blockquote can contain headings and other formatting.

## Syntax-Highlighted Code

### JavaScript/TypeScript

```javascript
// Example JavaScript function
function greetUser(name, age = 18) {
  const message = `Hello, ${name}! You are ${age} years old.`;
  console.log(message);
  return message;
}

// Using the function
const user = {
  name: "Alex",
  age: 25
};

greetUser(user.name, user.age);
```

### React Component

```tsx
import React, { useState, useEffect } from 'react';

interface UserProps {
  name: string;
  age: number;
}

const UserCard: React.FC<UserProps> = ({ name, age }) => {
  const [isActive, setIsActive] = useState(false);

  useEffect(() => {
    console.log(`User ${name} loaded`);
  }, [name]);

  return (
    <div className={`user-card ${isActive ? 'active' : ''}`}>
      <h3>{name}</h3>
      <p>Age: {age}</p>
      <button onClick={() => setIsActive(!isActive)}>
        {isActive ? 'Deactivate' : 'Activate'}
      </button>
    </div>
  );
};

export default UserCard;
```

### CSS Styles

```css
/* Example CSS styles */
.user-card {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
}

.user-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
}

.user-card.active {
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
}

@media (max-width: 768px) {
  .user-card {
    padding: 15px;
    margin: 10px;
  }
}
```

## Tables

| Name | Description | Status | Date |
|------|-------------|--------|------|
| Project A | Web Application | ✅ Active | 2024-01-15 |
| Project B | Mobile Application | 🚧 In Development | 2024-02-01 |
| Project C | API Service | ❌ Completed | 2024-01-30 |

### Table with Alignment

| Left | Center | Right |
|:------|:-----:|-------:|
| Left text | Center text | Right text |
| More text | More text | More text |

### Links

- [Next.js Official Website](https://nextjs.org/)
- [MDX Documentation](https://mdxjs.com/)
- [Tailwind CSS](https://tailwindcss.com/)

### Images

![Logo](/logo.svg)

## Horizontal Line

---

## Emojis and Symbols

🎉 🚀 💻 ⚡ 🔥 📱 🎨 🌟 ✨ 🎯

## Special Characters

- `<` for less than
- `>` for greater than
- `&` for ampersand
- `"` for quotes
- `'` for apostrophe

## Mermaid Diagrams

### Process Flowchart

```mermaid
%% zoom: true
%% height: 60vh
%% zoom-min: 0.5
%% zoom-max: 3
%% zoom-initial: 0.8
%% tooltip: This diagram shows the Probo system architecture
flowchart TD
    A[Start] --> B{Condition Check}
    B -->|Yes| C[Execute Action]
    B -->|No| D[Skip]
    C --> E[Update Data]
    D --> E
    E --> F{More Tasks?}
    F -->|Yes| B
    F -->|No| G[End]
```

### Sequence Diagram

```mermaid
%% zoom: true
%% height: 60vh
%% zoom-min: 0.5
%% zoom-max: 3
%% zoom-initial: 0.8
%% disable-tooltip: true
sequenceDiagram
    participant U as User
    participant F as Frontend
    participant B as Backend
    participant D as Database

    U->>F: Data Request
    F->>B: API Call
    B->>D: SQL Query
    D-->>B: Result
    B-->>F: JSON Response
    F-->>U: Display Data
```

### Class Diagram

```mermaid
%% zoom: true
%% height: 60vh
%% zoom-min: 0.5
%% zoom-max: 3
%% zoom-initial: 0.8
%% disable-tooltip: true
classDiagram
    class User {
        +String name
        +String email
        +Date createdAt
        +login()
        +logout()
        +updateProfile()
    }
    
    class Order {
        +String id
        +Number amount
        +Date orderDate
        +createOrder()
        +cancelOrder()
    }
    
    class Product {
        +String name
        +Number price
        +String description
        +updatePrice()
    }
```

### State Diagram

```mermaid
%% zoom: true
%% height: 60vh
%% zoom-min: 0.5
%% zoom-max: 3
%% zoom-initial: 0.8
%% disable-tooltip: true
stateDiagram-v2
    [*] --> Draft
    Draft --> Review : submit
    Review --> Approved : approve
    Review --> Rejected : reject
    Approved --> Published : publish
    Rejected --> Draft : revise
    Published --> [*]
```

### Deployment Diagram

```mermaid
%% zoom: true
%% height: 60vh
%% zoom-min: 0.5
%% zoom-max: 3
%% zoom-initial: 0.8
%% disable-tooltip: true
graph TB
    subgraph "Frontend"
        A[Next.js App]
        B[Vercel CDN]
    end
    
    subgraph "Backend Services"
        C[API Gateway]
        D[Auth Service]
        E[Data Service]
    end
    
    subgraph "Database"
        F[(PostgreSQL)]
        G[(Redis Cache)]
    end
    
    A --> B
    B --> C
    C --> D
    C --> E
    D --> F
    E --> F
    E --> G
```

## Admonitions (Информационные блоки)

<admonition type="info" title="Информация">
Это информационный блок с синим цветом. Используется для предоставления дополнительной информации.
</admonition>

<admonition type="success" title="Успех">
Это блок успеха с зеленым цветом. Используется для отображения положительных результатов или советов.
</admonition>

<admonition type="warning" title="Предупреждение">
Это блок предупреждения с желтым цветом. Используется для привлечения внимания к важным моментам.
</admonition>

<admonition type="danger" title="Опасно">
Это блок опасности с красным цветом. Используется для предупреждения о критических моментах или запретах.
</admonition>

<admonition type="info">
Блок без заголовка - просто информационное сообщение.
</admonition>

## HTML Elements

<div style="background: linear-gradient(45deg, #ff6b6b, #4ecdc4); padding: 20px; border-radius: 10px; color: white; text-align: center; margin-bottom: 20px;">
  <h3>Custom HTML Block</h3>
  <p>This is an example of using HTML in MDX</p>
</div>

### Setup Instructions

<admonition type="info" title="Important">
Before starting work, make sure you have Bun installed and all dependencies.
</admonition>

<admonition type="success" title="Ready">
The project has been successfully configured and is ready to work!
</admonition>

### Warnings

<admonition type="warning" title="Attention">
Do not delete files from the `content/` folder without prior backup.
</admonition>

<admonition type="danger" title="Caution">
Incorrect configuration may lead to data loss.
</admonition>

### Block without title

<admonition type="info">
This block demonstrates the ability to create informational messages without a title. Useful for additional notes or explanations.
</admonition>

## Conclusion

This file demonstrates all the main MDX content formatting capabilities. All styles should display correctly with syntax highlighting for code.
